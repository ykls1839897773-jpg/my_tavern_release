# 发布区 (Release)

这个仓库 (`my_tavern_release`) 是一个自动化发布仓库。

* **目的**：仅用于存放最终打包好的前端文件（`dist/`），供 jsDelivr CDN 加速使用。
* **来源**：所有的代码实际上都来自于我的私有开发仓库。
* **使用方法**：请直接使用 jsDelivr 链接引用本仓库的文件。

## 目录结构

(这里将会在我们第一次推送 dist 内容后自动填充)
